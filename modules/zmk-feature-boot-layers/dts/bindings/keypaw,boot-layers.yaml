description: Layers to activate once at boot

compatible: "keypaw,boot-layers"

properties:
  layers:
    type: array
    required: true
    description: |
      Keymap layer IDs to activate once at boot.
